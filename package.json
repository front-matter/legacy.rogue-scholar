{
  "name": "rogue-scholar",
  "private": true,
  "scripts": {
    "build": "next build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo next dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
