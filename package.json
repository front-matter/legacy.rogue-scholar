{
  "name": "rogue-scholar",
  "private": true,
  "scripts": {
    "build": "next build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "prettier": "^2.7.1",
    "prettier-plugin-tailwindcss": "^0.1.13",
    "turbo": "^1.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
